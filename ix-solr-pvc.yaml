apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ix-solr-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "gp2"
  resources:
    requests:
      storage: 512Mi