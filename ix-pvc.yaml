apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ix-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "gp2"
  resources:
    requests:
      storage: 2500Mi